import 'package:flutter/cupertino.dart';

class RepositoryScop extends StatelessWidget {
  final Widget child;
  const RepositoryScop({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
